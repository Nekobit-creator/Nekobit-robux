<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Robux Gifting Simulator</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f1f1f1;
      margin: 0;
    }
    header {
      background-color: #23272a;
      padding: 20px;
      color: white;
      text-align: center;
    }
    nav {
      display: flex;
      background-color: #2c2f33;
    }
    nav button {
      flex: 1;
      padding: 15px;
      background-color: #2c2f33;
      color: white;
      border: none;
      cursor: pointer;
      font-size: 16px;
    }
    nav button:hover, nav button.active {
      background-color: #7289da;
    }
    .tab {
      display: none;
      padding: 30px;
      background: white;
    }
    .tab.active {
      display: block;
    }
    .form-box {
      max-width: 400px;
      margin: 0 auto;
      background: #f9f9f9;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }
    label {
      font-weight: bold;
    }
    input, button {
      width: 100%;
      padding: 10px;
      margin: 10px 0 20px;
      border: 1px solid #ccc;
      border-radius: 6px;
